<h1>Books</h1>
<%= form_with model:@user, url:"/users/#{@user.id}", local:true do |f| %>
  <h4>Title</h4>
  <%= f.text_field :profile_image %>
  <h4>Opinion</h4>
  <%= f.text_area :name %>

 <br><%= link_to "show", user_path(@user.id) %>

<% end %>

<!--  <= form_with model:@user, local:true do |f| %>-->

<!-- <= attachment_image_tag @user, :profile_image %>-->
<!--  <label for="inputImage"></label>-->
<!--  <= f.attachment_field :profile_image_id, placeholder: "プロフィール画像" %>-->
<!--  <= f.submit "変更を保存" %>-->
<!--< end %>-->
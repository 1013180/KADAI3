 <h2>User info</h2>
 　<p><%= flash[:notice] %></p>
    <% if flash[:notice] %>
     <p id="notice">
       <%= flash[:notice] %>
     </p>
   <% end %>
   <%= form_with model:@user, local:true do |f| %>
   <%= attachment_image_tag @user, :profile_image, :fill, 60, 60, fallback: "no_image.jpg" %>
    <table>

    <tr><th><label for="inputName">Name</label></th><td>
   <%= f.text_field :name, autofocus: true, id:"inputName", placeholder:"name"%></td></tr>

   <tr><th><label for="inputIntroduction">introduction</label></th><td>
   <%= f.text_field :introduction, autofocus: true, id:"inputIntroduction", placeholder:""%></td></tr>

</table>

   <%=link_to "edit", edit_user_path(@user.id) %>

<% end %>

<h1>New book</h1>
<%= form_with model: @book,url: books_path, local: true do |f| %>
  <h4>Title</h4>
  <%= f.text_field :title %>
  <h4>Opinion</h4>
  <%= f.text_field :body %>
  <%= f.submit 'Create Book' %>
<% end %>



<h2>Books</h2>
  <table>
<thead>
<th></th>
<th>title</th>
<th>body</th>
<th></th>
</tr>
</thead>

<tbody>
<% @books.each do |book| %>
<tr>
<td>
<%= attachment_image_tag @user, :profile_image, :fill, 60, 60, fallback: "no_image.jpg" %>
</td>
<td>
<%= link_to book.title, book_path(book.id) %>
</td>
<td>
<%= book.body %>
</td>
</tr>
<% end %>
</tbody>
</table>
</div>


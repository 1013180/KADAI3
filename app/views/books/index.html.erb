
  <div class="row">

    <div class="col-2">
       <h2>User info</h2>
       <%= attachment_image_tag @user, :profile_image, :fill, 60, 60, fallback: "no_image.jpg" %>
        <table class="table table-striped">

    <tr>
    <th><label for="inputName">Name</label></th>
   <!--<= f.text_field :name, autofocus: true, id:"inputName", placeholder:"name"%></td></tr>-->
     <th><%=@user.name %>
     </th></tr>
   <tr><th><label for="inputIntroduction">introduction</label></th>
   <!--<= f.text_field :introduction, autofocus: true, id:"inputIntroduction", placeholder:""%></td></tr>-->
     <th><%=@user.introduction %>
     </th></tr>

    </table>
        <%=link_to edit_user_path(@user.id) do %>
　　　　<a class="btn btn-outline-secondary btn-block fas fa-user-cog edit_user_14861">edit</a>
　　　　<% end %>
      <%=link_to "edit", edit_user_path(@user.id), class: "btn btn-secondary" %>
    <br></br>

    <h2>New book</h>
    <%= form_with model: @book, local: true do |f| %>
      <h4>Title</h4>
      <%= f.text_field :title %>
      <h4>Opinion</h4>
      <%= f.text_field :body %>
      <%= f.submit 'Create Book' %>
    <% end %>
    </div>
<div class="col-8 offset-md-2">
<h2>Books</h2>

<table class="table">
<thead>
<tr>
<th></th>
<th>title</th>
<th>Opinion</th>
<th></th>
</tr>
</thead>

<tbody>
<% @books.each do |book| %>
<tr>
<td>
<%=link_to book_path(book.id) do %>
<%= attachment_image_tag @user, :profile_image, :fill, 100, 100 %>
<% end %>
</td>
<td>
<%= link_to edit_book_path(book.id) do %>
<%= book.title %>
<% end %>
</td>
<td>
<%= book.body %>
</td>



<% end %>
</tr>
</tbody>
</table>
</div>
  </div>  <!-- この行を追加 -->
</div>
   </div>  <!-- この行を追加 -->
    </div>